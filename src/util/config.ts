export const config = {
    QUERY_PARAM: "?api_key=<API_KEY>",
    API_URL: "https://api.com/",
};

export const route_paths = {
    DASHBOARD: "/",
    ESCROOM: "/room/:id",
    QUIZ: "/room/:id/quiz/:id",
};
